package ifes.flat.rl;

import ifes.flat.rl.MooreMachine;
import ifes.flat.rl.MealyMachine;

/**
 *
 * @author jefferson
 */
public class MealyMooreTransformer {

    public static <S, A, B> MooreMachine<S, A, B> mealyToMoore(MealyMachine<S, A, B> m) {
        throw new UnsupportedOperationException("Operação ainda não implementada.");
    }
    
    public static <S, A, B> MealyMachine<S, A, B> mooreToMealy(MooreMachine<S, A, B> m) {
        throw new UnsupportedOperationException("Operação ainda não implementada.");
    }
    
}
